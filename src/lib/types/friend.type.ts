export type FriendType = {
  id: string;
  name: string;
  image?: string;
};

export type FriendRequestType = {
  id: string;
  status: string;
  sender: string;
  name: string;
  image?: string;
};
